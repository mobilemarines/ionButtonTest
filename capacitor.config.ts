import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.ionButtonTest',
  appName: 'IonButtonTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
